# website/ — Agent 原则

## 职责

产品**营销站 / 官网**的工作区占位。

## 原则

1. init 阶段**不**安装站点框架、不生成业务页面。
2. 未来站点工程与内容应落在本目录（或本目录约定子结构），不要散落到 `app/` 或 `gtm/`。
3. `app/` 是产品应用本体；`website/` 是对外营销站——二者不要混目录。
4. SEO 文案草稿若仅用于上架/社媒，优先 `gtm/`；站点特有文案可在本目录内容层维护（有代码后）。
5. 本文件不指定 Next/Astro 等技术选型。
