# Routing mode：三选一 + 一行副文

Settings **Routing mode ›** 为 Auto / Global / Direct 单选，各附一行意图副文。不做长对比表、不做「当前生效规则」摘要墙、不在根页用 segmented 替代二级（与 DNS/Overrides › 一致）。

**为何：** 模式低频；用户要的是改意图，不是读分流教材。规则细节属订阅/服务商，产品不承诺自建全球引擎。

**后果：** 见 CONTEXT **Routing Mode Entry**；英文副文 hi-fi 期打磨。
