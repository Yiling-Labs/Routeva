# Auto = 服务商规则优先 + 客户端智能选节点

默认 Auto 不重建一套客户端全球分流库。路由以订阅/服务商下发的可解析规则与节点分组为主；客户端智能集中在节点测试、评分、自动选择与故障切换，并保留局域网等安全兜底。流媒体解锁等不承诺为客户端能力。用户/Agent 仅可增加少量可回滚的结构化覆盖（User Override Rule）。这样减少与机场配置冲突，并把工程重点放在 Self-Healing Loop 与稳定连接上。
