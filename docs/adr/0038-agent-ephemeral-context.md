# Agent 信任主张：临时结构化上下文，非任意文件

Agent 信任条与 *How we use data* 采用 **Ephemeral Analysis Context**：Help 内 **默认 Cloud On**（见 **0042**）时条为临时分析、不落盘；Cloud Off 声明本机、不发送。仅脱敏结构化上下文。不提供任意配置/日志文件上传；不写绝对「永不离端」同时默认上云。

**为何：** 对齐「临时分析不落盘」与低摩擦默认开云；避免 *file* 用语诱发错误心智。

**后果：** 见 CONTEXT **Cloud AI** / **Agent Surface**；实现须保证云侧无长期存会话原文与原始订阅。
