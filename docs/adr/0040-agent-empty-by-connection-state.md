# Help 空态随连接真值双态

Agent Surface 空态不统一写 *Trouble connecting?*。**未连上 / Can’t connect** 用故障向标题与 chips；**Connected** 用中性求助标题与策略向 chips。避免成功态进 Help 仍像「只会报错」。

**为何：** Help 顶栏随时可进（含绿场）；Connection Help 含失败求助与已连后的安全策略协助。

**后果：** 见 CONTEXT **Agent Surface**；hi-fi `06-agent.html` 须有两帧空态。
